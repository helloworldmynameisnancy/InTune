# Config Setup

To configure API keys:

1. Copy `APIKeys.swift.example` to `APIKeys.swift`
2. Replace the placeholder with your News API key from [newsapi.ai](https://newsapi.ai/)

The `APIKeys.swift` file is ignored by git to keep your keys private.
